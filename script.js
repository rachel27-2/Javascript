function subtract(num1, num2) {
    return num1 - num2;
}

welcome = (name, age) => {
    return `my name is ${name} & I am ${age} years old`;
}

powerUp = (num1, num2) => {
    return Math.pow(num1, num2);
}